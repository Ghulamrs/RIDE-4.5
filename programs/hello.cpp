// hello.cpp - the first C++ program: Build > Run File runs it.
#include <cstdio>
#include <string>

int main() {
    std::string who = "C++";
    std::printf("Hello from %s\n", who.c_str());
    return 0;
}
